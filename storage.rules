rules_version = '2';

// Regras dos arquivos (Storage): só quem está logado envia/lê.
// (Podemos deixar ainda mais restrito por usuário numa etapa futura.)
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
